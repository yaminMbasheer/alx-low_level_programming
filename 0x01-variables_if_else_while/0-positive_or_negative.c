#include <stdlib.h>
#include <time.h>
/* more headers goes there */
/* betty style doc for function main goes there */
int main(void)
{
int n;
srand(time(0));
n = rand() - RAND_MAX / 2;
/* your code goes there */
 if (j > 0)
{
printf(" %d positive  \n", j);
}
else if (j == 0)
{
printf(" %d  zero \n", j);
}
else
{
printf("%d nagetive  \n", j);
}
return (0);
}
